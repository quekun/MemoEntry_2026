1）为游戏添加了背景音乐，实现了在游戏设置界面控制背景音乐、音效、镜头跟随的实际内容，而不只是改变ui文本。



2）通过创建USaveGame子类实现了存档与读档系统，将局内记录数据以文件形式存放到游戏外指定路径的文件夹内，如果该路径下没有该文件夹，创建一个再放入存档文件



3）遇到了目前没有解决的bug，关于音频，第一次进入主菜单关卡，也就是ui界面时，设置界面控制音乐的按钮失灵，另外还有一个bug：读档后，存档里面音乐关闭，但是在主界面读取时仍然有音乐，但控制音乐的逻辑不可能写在主界面，这就需要主界面通过实际来触发一个函数，思来想去还是算了，本身存档改变音乐就没必要，与其让它成为bug，不如不写这个功能，这是属于设计上的失误，以后要争取避免

